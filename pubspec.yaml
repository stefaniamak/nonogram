name: nonogram
description: A nonogram puzzle solver by Stefania Makrygiannaki as a Uni thesis.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+2

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.4
  built_collection: ^5.1.1
  dio: ^5.3.3
  flutter_bloc: ^8.1.6
  sliver_tools: ^0.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  isolate_manager: ^5.2.1+1
  json_annotation: ^4.9.0
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
