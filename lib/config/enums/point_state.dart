/// An enumeration representing the state of a point in a Nonogram.
enum PointState {
  /// The state of the point is unknown ("?").
  unknown,

  /// The point is filled ("1").
  filled,

  /// The point is marked with a cross ("0").
  cross
}
